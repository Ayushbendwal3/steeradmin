import React from 'react'
import { SectionProps } from '../Stepper/Stepper.types'

const ConfirmationSection: React.FC<SectionProps> = ({
  setFormStage,
  formStage,
}) => {
  return <div>ConfirmationSection</div>
}

export default ConfirmationSection
